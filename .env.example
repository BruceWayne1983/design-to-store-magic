# Supabase (publishable / anon key — safe in client bundle, RLS enforced server-side)
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Shopify Storefront (publishable token — designed for client use)
VITE_SHOPIFY_STORE_DOMAIN=""
VITE_SHOPIFY_STOREFRONT_TOKEN=""

# Soft password gate for /home and downstream routes. Leave unset to keep the
# default. Note: this is shipped in the client bundle either way — for real
# access control gate the deploy at the platform level.
VITE_SITE_PASSWORD=""
